{
  "homepage": "https://iamshatisneupane.github.io/",

  "dependencies": {
    "express": "^4.18.2",
    "nodemailer": "^6.9.1",
    "path": "^0.12.7",
    "xoauth2": "^1.2.0"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  },

  "scripts": {
    "build": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
